{% extends 'base.html.twig' %}

{% block title %}Edition Annonce{% endblock %}

{% block body %}
{% include 'components/_header.html.twig' %}
<main role="main">
  {% include 'components/_intro.html.twig' %}

    <div class="text-center pb-5">
        <div>
            <h1 class="display-5 m-0">Edition d'annonce</h1>
        </div>
        {# <a href="{{ path('app_home') }}" title="Retour à la liste" class="btn btn-outline-secondary">
            Retour à la liste
        </a> #}
    </div>

    <div class="container mx-auto">
        {{ form(form) }}
    </div>
</main>
{% endblock %}